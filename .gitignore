node_modules
.DS_Store
.env*
.vercel

# Capacitor web-root build artifact (regenerated from index.html by `npm run copy:www`)
www

# Xcode CLI build output (xcodebuild -derivedDataPath build)
build
!scripts/.env.release.example
